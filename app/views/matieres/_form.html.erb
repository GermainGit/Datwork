<%= form_with(model: matiere, local: true) do |form| %>
  <% if matiere.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(matiere.errors.count, "error") %> prohibited this matiere from being saved:</h2>

      <ul>
      <% matiere.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nom %>
    <%= form.text_field :nom, id: :matiere_nom %>
  </div>

  <div class="field">
    <p>Couleur :</p>
    <%= form.collection_select :color_id, Color.all, :id, :nom%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
